flowchart TD
    START([开始]) --> INIT[初始化任务]
    INIT --> CHECK_LOGIN{检查登录状态}

    CHECK_LOGIN -->|未登录| WAIT_LOGIN[等待用户手动登录]
    WAIT_LOGIN --> CHECK_LOGIN
    CHECK_LOGIN -->|出现验证码/风控| BLOCKED[任务阻断]
    CHECK_LOGIN -->|已登录| READ_PAGE[读取当前职位页面]

    READ_PAGE --> OBSERVE[获取页面观察结果]
    OBSERVE --> DETECT_PAGE{识别页面状态}

    DETECT_PAGE -->|访问受限| BLOCKED
    DETECT_PAGE -->|登录失效| WAIT_LOGIN
    DETECT_PAGE -->|职位页面| EXTRACT_JOB[提取职位信息]
    DETECT_PAGE -->|页面异常| DIAGNOSE[保存截图和诊断信息]
    DIAGNOSE --> FAILED[任务失败]

    EXTRACT_JOB --> NORMALIZE[职位信息结构化]
    NORMALIZE --> DEDUP{是否已投递}
    DEDUP -->|是| SKIP_DUPLICATE[跳过重复职位]
    DEDUP -->|否| HARD_MATCH[硬条件筛选]

    HARD_MATCH -->|不符合| SKIP[跳过职位]
    HARD_MATCH -->|符合| LLM_ANALYZE[LLM 分析岗位匹配度]

    LLM_ANALYZE --> ANALYSIS_RESULT{是否推荐}
    ANALYSIS_RESULT -->|不推荐| SKIP
    ANALYSIS_RESULT -->|推荐| GENERATE_MESSAGE[生成针对性话术]

    GENERATE_MESSAGE --> REVIEW[展示职位/匹配结果/话术]
    REVIEW --> HUMAN_CONFIRM{人工确认}

    HUMAN_CONFIRM -->|跳过| SKIP
    HUMAN_CONFIRM -->|修改话术| EDIT_MESSAGE[用户编辑话术]
    EDIT_MESSAGE --> REVIEW
    HUMAN_CONFIRM -->|确认投递| VERIFY_TARGET[再次校验公司和职位]

    VERIFY_TARGET -->|校验失败| FAILED
    VERIFY_TARGET -->|校验成功| FILL_FORM[填充话术和简历]
    FILL_FORM --> FINAL_CONFIRM{最终人工确认}

    FINAL_CONFIRM -->|取消| SKIP
    FINAL_CONFIRM -->|确认发送| SUBMIT[执行一次投递]

    SUBMIT --> VERIFY_RESULT{检查投递结果}
    VERIFY_RESULT -->|成功| RECORD_SUCCESS[保存成功记录]
    VERIFY_RESULT -->|已投递| RECORD_DUPLICATE[保存重复记录]
    VERIFY_RESULT -->|验证码/风控| BLOCKED
    VERIFY_RESULT -->|普通错误| RETRY{是否可以重试}

    RETRY -->|是| READ_PAGE
    RETRY -->|否| FAILED

    RECORD_SUCCESS --> END([结束])
    RECORD_DUPLICATE --> END
    SKIP_DUPLICATE --> END
    SKIP --> END
    BLOCKED --> END
    FAILED --> END